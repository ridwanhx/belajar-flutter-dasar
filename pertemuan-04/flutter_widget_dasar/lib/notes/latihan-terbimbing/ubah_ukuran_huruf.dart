import 'package:flutter/material.dart';

void main() => runApp(
  const MaterialApp(
    home: Scaffold(
      body: Center(
        child: Text('Halo semua', style: TextStyle(
          color: Colors.indigo,
          fontSize: 28,
          fontWeight: FontWeight.w700
        ),)
        )
      ),
  ),
);
